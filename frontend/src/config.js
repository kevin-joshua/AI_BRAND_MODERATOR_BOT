const config = {
    API_BASE_URL: 'http://127.0.0.1:8000/', // Update this with your actual backend URL
    ENDPOINTS: {
        SCRAPE: 'scrape',
        QUERY: 'query',
        UPLOAD: 'upload_pdf'
    }
};

export default config; 